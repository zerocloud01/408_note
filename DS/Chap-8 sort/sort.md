# 排序
内部排序：数据均在内存中
外部排序：数据过多，无法全部放入内存
## 插入排序
可以每次插入可以二分优化

## 希尔排序
不稳定
每次设定一个增量 $d$，将表分割为若干子表进行插入排序，然后循环让 $d \leftarrow  d/2$，继续排序，直到 $d = 1$

复杂度无法估计，最坏 $O(n^2)$，$n$ 在某个范围内可以达到 $O(n^{1.3})$
> $d_0$ 最好设置为 $n/2$

## 快速排序

## 简单选择排序

## 选择排序

## 堆排序
将数组视作二叉树（子节点为2i与2i+1）

根节点大于左右子树