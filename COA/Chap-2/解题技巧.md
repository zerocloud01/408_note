## 运算方法和运算电路
2.2 P44
### 05
算数移位和逻辑移位由**数据类型决定**，有符号数固定是算数移位

### 10
$n$ 位补码最高位要存储符号

### 11
**存储**时而不是运算时，**正确的**数据只需要一个符号位，因为双符号位是相同的。

### 24
补码除法器（原码除法器也是）会先判断**被除数的==绝对值==**是否**小于** **除数的==绝对值==**

### 32
CF表示位是判断作为无符号时时候溢出，虽然其叫做进位/借位标识位